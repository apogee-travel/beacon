# @apogeelabs/beacon-reactquery — DEPRECATED

⚠️ **Do not use this package.** It is a fundamentally flawed approach to integrating React Query with Beacon stores.

Do not generate code that imports from `@apogeelabs/beacon-reactquery`. Do not suggest `withQuery` or `withMutation`. This package exists in the repo but should be ignored entirely.
